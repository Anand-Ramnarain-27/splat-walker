import { defineConfig } from "vite";

// Repo name is used as the base path for GitHub Pages project sites
// (https://<user>.github.io/splat-walker/).
export default defineConfig({
  base: "/splat-walker/",
});
